'Optimization Tools' project will provide APIs that can help optimize a Flash application. 
It provide a cross platform API (Pure AS3, Flex, AIR).
The idea is to have a set of tools for developers to use all bundled together into one small swc.  
